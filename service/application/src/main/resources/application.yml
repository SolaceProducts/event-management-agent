springdoc:
  packages-to-scan: com.solace.maas.ep.event.management.agent.scanManager.rest
  api-docs:
    path: /docs/event-management-agent
  swagger-ui:
    path: /event-management-agent/swagger-ui.html
server:
  port: 8180
spring:
  main:
    allow-bean-definition-overriding: true
  servlet:
    multipart:
      max-request-size: ${MAX_REQUEST_SIZE:5MB}
      max-file-size: ${MAX_FILE_SIZE:5MB}
  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.H2Dialect
  datasource:
    url: jdbc:h2:file:./data/cache;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    driver-class-name: org.h2.Driver
    password: password
  h2:
    console:
      settings:
        web-allow-others: true
      enabled: true
      path: /h2
camel:
  springboot:
    use-mdc-logging: true
kafka:
  client:
    config:
      reconnections:
        max-backoff:
          unit: milliseconds
          value: 1000
        backoff:
          unit: milliseconds
          value: 50
      connections:
        max-idle:
          unit: milliseconds
          value: 10000
        request-timeout:
          unit: milliseconds
          value: 5000
        timeout:
          unit: milliseconds
          value: 60000
eventPortal:
  topicPrefix: ${EP_TOPIC_PREFIX:sc/ep/runtime}
  runtimeAgentId: ${EP_RUNTIME_AGENT_ID:bggm7tez0rx}
  organizationId: ${EP_ORGANIZATION_ID:localrcorg}
  gateway:
    id: 76bostklti9
    name: evmrcreatedwithlemmy
    messaging:
      standalone: false
      rtoSession: false
      enableHeartbeats: true
      testHeartbeats: true
      connections:
        - name: eventPortalGateway
          authenticationType: ${EP_GATEWAY_AUTH:basicAuthentication}
          msgVpn: ${EP_GATEWAY_MSGVPN:rcbroker2}
          url: ${EP_GATEWAY_URL:tcps://mr-connection-z4nfhexqyix.messaging.solace.cloud:55443}
          users:
            - clientName: client_bggm7tez0rx
              username: ${EP_GATEWAY_USERNAME:localrcorg-v6693yiarsp-bggm7tez0rx}
              password: k#8^P2shCXI26SzgC5X6cbxb
              name: messaging1
plugins:
  resources:
    - id: 26dn6j9g9s0
      type: solace
      name: rcbroker2
      connections:
        - name: rcbroker2
          url: https://mr-connection-z4nfhexqyix.messaging.solace.cloud:943
          properties:
            - name: msgVpn
              value: rcbroker2
            - name: sempPageSize
              value: 100
          authentication:
            - properties:
                - name: type
                  value: basicAuthentication
              protocol: semp
              credentials:
                - properties:
                    - name: username
                      value: rcbroker2-admin
                    - name: password
                      value: 34nik7ec63b2pdeoti7s874pfn
                  source: ENVIRONMENT_VARIABLE
                  operations:
                    - name: ALL
    - id: urjdc83hox6
      type: solace
      name: rchakivmr
      connections:
        - name: rchakivmr
          url: https://mr-connection-l80mies0a1n.messaging.solace.cloud:943
          properties:
            - name: msgVpn
              value: rchakivmr
            - name: sempPageSize
              value: 100
          authentication:
            - properties:
                - name: type
                  value: basicAuthentication
              protocol: semp
              credentials:
                - properties:
                    - name: username
                      value: rchakivmr-admin
                    - name: password
                      value: kcfot94d1gronuv34b7kmpvtm6
                  source: ENVIRONMENT_VARIABLE
                  operations:
                    - name: ALL
