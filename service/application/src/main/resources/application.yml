springdoc:
  packages-to-scan: com.solace.maas.ep.event.management.agent.scanManager.rest
  api-docs:
    path: /docs/event-management-agent
  swagger-ui:
    path: /event-management-agent/swagger-ui.html
server:
  port: 8180
spring:
  main:
    allow-bean-definition-overriding: true
  servlet:
    multipart:
      max-request-size: ${MAX_REQUEST_SIZE:5MB}
      max-file-size: ${MAX_FILE_SIZE:5MB}
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
  datasource:
    url: jdbc:h2:file:./data/cache;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    driver-class-name: org.h2.Driver
    password: 0NctlP6R5h
  h2:
    console:
      settings:
        web-allow-others: true
      enabled: true
      path: /h2
camel:
  springboot:
    use-mdc-logging: true
kafka:
  client:
    config:
      reconnections:
        max-backoff:
          unit: milliseconds
          value: 1000
        backoff:
          unit: milliseconds
          value: 50
      connections:
        max-idle:
          unit: milliseconds
          value: 10000
        request-timeout:
          unit: milliseconds
          value: 5000
        timeout:
          unit: milliseconds
          value: 60000
eventPortal:
  runtimeAgentId: ${EP_RUNTIME_AGENT_ID:tz2q285acyk}
  organizationId: ${EP_ORGANIZATION_ID:localrcorg}
  topicPrefix: ${EP_TOPIC_PREFIX:sc/ep/runtime}
  gateway:
    id: kz7hk9rgv67
    name: rchakraborty-evmr-2
    messaging:
      standalone: false
      rtoSession: false
      enableHeartbeats: true
      testHeartbeats: true
      connections:
        - name: eventPortalGateway
          authenticationType: ${EP_GATEWAY_AUTH:basicAuthentication}
          msgVpn: ${EP_GATEWAY_MSGVPN:rchakraborty-evmr-2}
          url: ${EP_GATEWAY_URL:tcps://mr-connection-q9bhkwb3dcx.messaging.solace.cloud:55443}
          users:
            - clientName: client_tz2q285acyk
              username: ${EP_GATEWAY_USERNAME:org-localrcorg-tf03isa8ibu-tz2q285acyk}
              password: ^OasJ{ngtQG=S.a5ZkJ@#sHp
              name: messaging1
plugins:
  resources:
    - id: cl8xupkqfuq
      type: solace
      name: rchakraborty-evmr-2
      connections:
        - name: rchakraborty-evmr-2
          url: https://mr-connection-q9bhkwb3dcx.messaging.solace.cloud:943
          properties:
            - name: msgVpn
              value: rchakraborty-evmr-2
            - name: sempPageSize
              value: 100
          authentication:
            - properties:
                - name: type
                  value: basicAuthentication
              protocol: semp
              credentials:
                - properties:
                    - name: username
                      value: rchakraborty-evmr-2-admin
                    - name: password
                      value: p0eqi0n34qlf3fbj39npskrp83
                  source: ENVIRONMENT_VARIABLE
