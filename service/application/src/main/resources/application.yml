springdoc:
  packages-to-scan: com.solace.maas.ep.event.management.agent.scanManager.rest
  api-docs:
    path: /docs/event-management-agent
  swagger-ui:
    path: /event-management-agent/swagger-ui.html
server:
  port: 8180
spring:
  h2:
    console:
      settings:
        web-allow-others: true
      enabled: true
      path: /h2
  main:
    allow-bean-definition-overriding: true
  servlet:
    multipart:
      max-file-size: ${MAX_FILE_SIZE:5MB}
      max-request-size: ${MAX_REQUEST_SIZE:5MB}
  jpa:
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  datasource:
    password: password
    url: jdbc:h2:file:./data/cache;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    driver-class-name: org.h2.Driver
camel:
  springboot:
    use-mdc-logging: true
kafka:
  client:
    config:
      connections:
        max-idle:
          value: 10000
          unit: milliseconds
        request-timeout:
          value: 5000
          unit: milliseconds
        timeout:
          value: 60000
          unit: milliseconds
      reconnections:
        max-backoff:
          value: 1000
          unit: milliseconds
        backoff:
          value: 50
          unit: milliseconds
eventPortal:
  runtimeAgentId: ${EP_RUNTIME_AGENT_ID:203os393ke1}
  organizationId: ${EP_ORGANIZATION_ID:localrcorg}
  topicPrefix: ${EP_TOPIC_PREFIX:sc/ep/runtime}
  gateway:
    id: kz7hk9rgv67
    name: rchakraborty-evmr
    messaging:
      standalone: false
      rtoSession: false
      enableHeartbeats: true
      testHeartbeats: true
      connections:
        - name: eventPortalGateway
          authenticationType: ${EP_GATEWAY_AUTH:basicAuthentication}
          msgVpn: ${EP_GATEWAY_MSGVPN:rchakraborty-evmr}
          url: ${EP_GATEWAY_URL:tcps://mr-connection-x1t4p6a5v0v.messaging.solace.cloud:55443}
          users:
            - name: messaging1
              clientName: client_203os393ke1
              username: ${EP_GATEWAY_USERNAME:localrcorg-v6693yiarsp-203os393ke1}
              password: y2idiUZa6v5orjd5Tj-ljyMB
plugins:
  resources:
    - id: css67wbyjnt
      type: solace
      name: CloudManagedBroker1
      connections:
        - name: POCCloudManagedEMAEVMR
          url: https://mr-connection-i41xmmlismp.messaging.solace.cloud:943
          properties:
            - value: poccloudmanagedemaevmr
              name: msgVpn
            - value: 100
              name: sempPageSize
          authentication:
            - properties:
                - value: basicAuthentication
                  name: type
              protocol: ''
              credentials:
                - properties:
                    - name: username
                      value: poccloudmanagedemaevmr-admin
                    - name: password
                      value: o9oerfkaqf5rgfs9h1h0s7bmjr
                  source: ENVIRONMENT_VARIABLE
                  operations:
                    - name: ALL
    - id: 26dn6j9g9s0
      type: solace
      name: SelfManagedBroker1
      connections:
        - name: rchakraborty-dev
          url: https://ep-ivmrs-for-devs.messaging.solace.cloud:943
          properties:
            - value: rchakraborty-dev
              name: msgVpn
            - value: 100
              name: sempPageSize
          authentication:
            - properties:
                - value: basicAuthentication
                  name: type
              protocol: semp
              credentials:
                - properties:
                    - name: username
                      value: rchakraborty-dev
                    - name: password
                      value: rchakraborty-dev
                  source: ENVIRONMENT_VARIABLE
                  operations:
                    - name: ALL
