springdoc:
  packages-to-scan: com.solace.maas.ep.event.management.agent.scanManager.rest
  api-docs:
    path: /docs/event-management-agent
  swagger-ui:
    path: /event-management-agent/swagger-ui.html
server:
  port: 8180
spring:
  main:
    allow-bean-definition-overriding: true
  servlet:
    multipart:
      max-request-size: ${MAX_REQUEST_SIZE:5MB}
      max-file-size: ${MAX_FILE_SIZE:5MB}
  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.H2Dialect
  datasource:
    url: jdbc:h2:file:./data/cache;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    driver-class-name: org.h2.Driver
    password: 4LJtvoDNEu
  h2:
    console:
      settings:
        web-allow-others: true
      enabled: true
      path: /h2
camel:
  main:
    use-mdc-logging: true
kafka:
  client:
    config:
      reconnections:
        max-backoff:
          unit: milliseconds
          value: 1000
        backoff:
          unit: milliseconds
          value: 50
      connections:
        max-idle:
          unit: milliseconds
          value: 10000
        request-timeout:
          unit: milliseconds
          value: 5000
        timeout:
          unit: milliseconds
          value: 60000
eventPortal:
  runtimeAgentId: ${EP_RUNTIME_AGENT_ID:6ua7m6gtphq}
  organizationId: ${EP_ORGANIZATION_ID:rudraneelorg}
  topicPrefix: ${EP_TOPIC_PREFIX:sc/ep/runtime}
  gateway:
    id: 955tk3b89kg
    name: eptestautomationsvc-pub
    messaging:
      standalone: false
      rtoSession: false
      enableHeartbeats: true
      testHeartbeats: true
      connections:
        - name: eventPortalGateway
          authenticationType: ${EP_GATEWAY_AUTH:basicAuthentication}
          msgVpn: ${EP_GATEWAY_MSGVPN:eptestautomationsvc-pub}
          url: ${EP_GATEWAY_URL:tcps://mr-connection-czvalyfsyzp.messaging.mymaas.net:55443}
          users:
            - clientName: client_6ua7m6gtphq
              username: ${EP_GATEWAY_USERNAME:org-rudraneelorg-chbco920o68-6ua7m6gtphq}
              password: _0qi@-$Bg_GeHgbfd4}P=xm5
              name: messaging1
plugins:
  resources:
    - id: goro90etuw7
      type: solace
      name: eptestautomationsvc-pub
      connections:
        - name: eptestautomationsvc-pub
          url: https://mr-connection-czvalyfsyzp.messaging.mymaas.net:943
          properties:
            - name: msgVpn
              value: eptestautomationsvc-pub
            - name: sempPageSize
              value: 100
          authentication:
            - properties:
                - name: type
                  value: basicAuthentication
              protocol: semp
              credentials:
                - properties:
                    - name: username
                      value: eptestautomationsvc-pub-admin
                    - name: password
                      value: d1jbjdrrf4nfi164pcifu33re7
                  source: ENVIRONMENT_VARIABLE
                  operations:
                    - name: ALL
