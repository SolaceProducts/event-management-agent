springdoc:
  packages-to-scan: com.solace.maas.ep.event.management.agent.scanManager.rest
  api-docs:
    path: /docs/event-management-agent
  swagger-ui:
    path: /event-management-agent/swagger-ui.html
server:
  port: 8180
spring:
  main:
    allow-bean-definition-overriding: true
  servlet:
    multipart:
      max-request-size: ${MAX_REQUEST_SIZE:5MB}
      max-file-size: ${MAX_FILE_SIZE:5MB}
  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.H2Dialect
  datasource:
    url: jdbc:h2:file:./data/cache;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    driver-class-name: org.h2.Driver
    password: password
  h2:
    console:
      settings:
        web-allow-others: true
      enabled: true
      path: /h2
camel:
  springboot:
    use-mdc-logging: true
kafka:
  client:
    config:
      reconnections:
        max-backoff:
          unit: milliseconds
          value: 1000
        backoff:
          unit: milliseconds
          value: 50
      connections:
        max-idle:
          unit: milliseconds
          value: 10000
        request-timeout:
          unit: milliseconds
          value: 5000
        timeout:
          unit: milliseconds
          value: 60000
eventPortal:
  topicPrefix: ${EP_TOPIC_PREFIX:sc/ep/runtime}
  runtimeAgentId: ${EP_RUNTIME_AGENT_ID:ixtl2wh2w3e}
  organizationId: ${EP_ORGANIZATION_ID:moodiorg}
  gateway:
    id: ebuy0bbaah8
    name: moodi-evmr
    messaging:
      standalone: false
      rtoSession: false
      enableHeartbeats: true
      testHeartbeats: true
      connections:
        - name: eventPortalGateway
          authenticationType: ${EP_GATEWAY_AUTH:basicAuthentication}
          msgVpn: ${EP_GATEWAY_MSGVPN:moodi-ivmr}
          url: ${EP_GATEWAY_URL:tcps://mr-connection-x5opw2q43tv.messaging.solace.cloud:55443}
          users:
            - clientName: client_ixtl2wh2w3e
              username: ${EP_GATEWAY_USERNAME:moodiorg-ucdds0exvz0-ixtl2wh2w3e}
              password: B_Mm9{CP70apBbl810QdaBQt
              name: messaging1
plugins:
  resources:
    - id: 47pok3f3swr
      type: solace
      name: moodiSolaceMS
      connections:
        - name: moodiSolaceMS
          url: http://moodisolace-solace.mymaas.net:8080
          properties:
            - name: msgVpn
              value: default
            - name: sempPageSize
              value: 100
          authentication:
            - properties:
                - name: type
                  value: basicAuthentication
              protocol: semp
              credentials:
                - properties:
                    - name: username
                      value: admin
                    - name: password
                      value: adminy
                  source: ENVIRONMENT_VARIABLE
                  operations:
                    - name: ALL
    - id: w4q6c44aae8
      type: kafka
      name: ms-try1
      connections:
        - name: kafkaNoAuthConnection
          url: ${KAFKA_BOOTSTRAP_SERVERS:kafka1:9092}
          authentication:
            - protocol: PLAINTEXT
              credentials:
                - type: noAuth
                  source: ENVIRONMENT_VARIABLE
                  operations:
                    - name: ALL
                  properties: [
                  ]
    - id: 0j4ogczadyw
      type: CONFLUENT_SCHEMA_REGISTRY
      name: ms-try1 schema
      relatedServices:
        - w4q6c44aae8
      connections:
        - name: ms-try1 schema connection
          url: ${CONFLUENT_SCHEMA_REGISTRY_URL:http://hk-discovery.mymaas.net:8081}
