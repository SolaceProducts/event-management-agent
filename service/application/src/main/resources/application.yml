springdoc:
  packages-to-scan: com.solace.maas.ep.event.management.agent.scanManager.rest
  api-docs:
    path: /docs/event-management-agent
  swagger-ui:
    path: /event-management-agent/swagger-ui.html
server:
  port: 8180
spring:
  jpa:
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  datasource:
    driver-class-name: org.h2.Driver
    password: ZabLB6Lv8J
    url: jdbc:h2:file:./data/cache;DB_CLOSE_ON_EXIT=FALSE
    username: sa
  h2:
    console:
      enabled: true
      path: /h2
      settings:
        web-allow-others: true
  main:
    allow-bean-definition-overriding: true
  servlet:
    multipart:
      max-file-size: ${MAX_FILE_SIZE:5MB}
      max-request-size: ${MAX_REQUEST_SIZE:5MB}
camel:
  springboot:
    use-mdc-logging: true
kafka:
  client:
    config:
      reconnections:
        max-backoff:
          value: 1000
          unit: milliseconds
        backoff:
          value: 50
          unit: milliseconds
      connections:
        max-idle:
          value: 10000
          unit: milliseconds
        request-timeout:
          value: 5000
          unit: milliseconds
        timeout:
          value: 60000
          unit: milliseconds
eventPortal:
  runtimeAgentId: ${EP_RUNTIME_AGENT_ID:dlco88t868f}
  organizationId: ${EP_ORGANIZATION_ID:eptestautomationorg}
  gateway:
    id: nysbg4g9c3g
    name: myEVMR
    messaging:
      standalone: false
      rtoSession: false
      enableHeartbeats: true
      testHeartbeats: true
      connections:
        - name: eventPortalGateway
          authenticationType: ${EP_GATEWAY_AUTH:basicAuthentication}
          msgVpn: ${EP_GATEWAY_MSGVPN:ep-perf-dev-evmr}
          url: ${EP_GATEWAY_URL:tcps://mr-connection-ag2uh04mojb.messaging.solace.cloud:55443}
          users:
            - password: u^G}ncT.69LNZyd=_r58_dJZ
              name: messaging1
              clientName: client_dlco88t868f
              username: ${EP_GATEWAY_USERNAME:org-eptestautomationorg-isvany43s8t-dlco88t868f}
  topicPrefix: ${EP_TOPIC_PREFIX:sc/ep/runtime}